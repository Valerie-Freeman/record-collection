# Python dependencies for the data-entry script in scripts/.
# The shipped site has no runtime dependencies; this is tooling only.
#   python3 -m pip install -r requirements.txt
#
# scripts/add_records.py needs a Postgres driver to insert into Supabase.
psycopg2-binary>=2.9
